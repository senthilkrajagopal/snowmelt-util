{{- if and .Values.observability.grafana.enabled .Values.observability.prometheus.enabled }}
# Grafana dashboard for nodeexp, wired against the
# `snowmelt` datasource UID. Companion: `grafana-dashboard-nodeexp-prometheus.yaml`.
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "snowmelt.fullname" . }}-grafana-dashboard-nodeexp-snowmelt
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "snowmelt.labels" . | nindent 4 }}
    grafana_dashboard: "1"
data:
  nodeexp-snowmelt.json: |-
{{ .Files.Get "files/nodeexp-snowmelt.json" | indent 4 }}
{{- end }}
