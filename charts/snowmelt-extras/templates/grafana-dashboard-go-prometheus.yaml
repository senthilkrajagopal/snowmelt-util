{{- if and .Values.observability.grafana.enabled .Values.observability.prometheus.enabled }}
# Grafana dashboard for go, wired against the
# `prometheus` datasource UID. Companion: `grafana-dashboard-go-snowmelt.yaml`.
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "snowmelt.fullname" . }}-grafana-dashboard-go-prometheus
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "snowmelt.labels" . | nindent 4 }}
    grafana_dashboard: "1"
data:
  go-prometheus.json: |-
{{ .Files.Get "files/go-prometheus.json" | indent 4 }}
{{- end }}
