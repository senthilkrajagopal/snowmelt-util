{{- if and .Values.observability.grafana.enabled .Values.observability.prometheus.enabled }}
# Grafana dashboard for jvm, wired against the
# `snowmelt` datasource UID. Companion: `grafana-dashboard-jvm-prometheus.yaml`.
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "snowmelt.fullname" . }}-grafana-dashboard-jvm-snowmelt
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "snowmelt.labels" . | nindent 4 }}
    grafana_dashboard: "1"
data:
  jvm-snowmelt.json: |-
{{ .Files.Get "files/jvm-snowmelt.json" | indent 4 }}
{{- end }}
