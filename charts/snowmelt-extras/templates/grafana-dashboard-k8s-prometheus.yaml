{{- if and .Values.observability.grafana.enabled .Values.observability.prometheus.enabled }}
# Grafana dashboard for k8s, wired against the
# `prometheus` datasource UID. Companion: `grafana-dashboard-k8s-snowmelt.yaml`.
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "snowmelt.fullname" . }}-grafana-dashboard-k8s-prometheus
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "snowmelt.labels" . | nindent 4 }}
    grafana_dashboard: "1"
data:
  k8s-prometheus.json: |-
{{ .Files.Get "files/k8s-prometheus.json" | indent 4 }}
{{- end }}
