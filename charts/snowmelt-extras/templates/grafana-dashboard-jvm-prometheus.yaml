{{- if and .Values.observability.grafana.enabled .Values.observability.prometheus.enabled }}
# Grafana dashboard for jvm, wired against the
# `prometheus` datasource UID. Companion: `grafana-dashboard-jvm-snowmelt.yaml`.
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "snowmelt.fullname" . }}-grafana-dashboard-jvm-prometheus
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "snowmelt.labels" . | nindent 4 }}
    grafana_dashboard: "1"
data:
  jvm-prometheus.json: |-
{{ .Files.Get "files/jvm-prometheus.json" | indent 4 }}
{{- end }}
