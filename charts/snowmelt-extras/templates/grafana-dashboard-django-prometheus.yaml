{{- if and .Values.observability.grafana.enabled .Values.observability.prometheus.enabled }}
# Grafana dashboard for django, wired against the
# `prometheus` datasource UID. Companion: `grafana-dashboard-django-snowmelt.yaml`.
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "snowmelt.fullname" . }}-grafana-dashboard-django-prometheus
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "snowmelt.labels" . | nindent 4 }}
    grafana_dashboard: "1"
data:
  django-prometheus.json: |-
{{ .Files.Get "files/django-prometheus.json" | indent 4 }}
{{- end }}
