{{- if and .Values.observability.grafana.enabled .Values.observability.prometheus.enabled }}
# Grafana dashboard for the rate() counter twins, wired against the
# `prometheus` datasource UID. Companion:
# `grafana-dashboard-wellformed-rate-snowmelt.yaml`.
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "snowmelt.fullname" . }}-grafana-dashboard-wellformed-rate-prometheus
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "snowmelt.labels" . | nindent 4 }}
    grafana_dashboard: "1"
data:
  wellformed-rate-prometheus.json: |-
{{ .Files.Get "files/wellformed-rate-prometheus.json" | indent 4 }}
{{- end }}
