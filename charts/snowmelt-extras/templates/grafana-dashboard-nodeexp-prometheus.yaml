{{- if and .Values.observability.grafana.enabled .Values.observability.prometheus.enabled }}
# Grafana dashboard for nodeexp, wired against the
# `prometheus` datasource UID. Companion: `grafana-dashboard-nodeexp-snowmelt.yaml`.
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "snowmelt.fullname" . }}-grafana-dashboard-nodeexp-prometheus
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "snowmelt.labels" . | nindent 4 }}
    grafana_dashboard: "1"
data:
  nodeexp-prometheus.json: |-
{{ .Files.Get "files/nodeexp-prometheus.json" | indent 4 }}
{{- end }}
