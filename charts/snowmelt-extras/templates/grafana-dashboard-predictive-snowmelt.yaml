{{- if and .Values.observability.grafana.enabled .Values.observability.prometheus.enabled }}
# 6-panel Phase η predictive + parameterized window-fn dashboard
# (snowmelt-pinned).
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "snowmelt.fullname" . }}-grafana-dashboard-predictive-snowmelt
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "snowmelt.labels" . | nindent 4 }}
    grafana_dashboard: "1"
data:
  snowmelt-predictive.json: |
    {
      "annotations": {"list": []},
      "editable": true, "id": null,
      "uid": "snowmelt-predictive",
      "title": "Snowmelt — predictive + parameterized (Phase η)",
      "tags": ["snowmelt", "promql", "predictive", "phase-eta"],
      "timezone": "", "schemaVersion": 38, "refresh": "30s",
      "time": {"from": "now-30m", "to": "now"},
      "templating": {"list": []},
      "panels": [
        {{- $panels := list
            (dict "title" "predict_linear(jvm_thread_count[5m], 60)"
                  "expr"  "predict_linear(jvm_thread_count[5m], 60)"
                  "x" 0  "y" 0)
            (dict "title" "predict_linear(jvm_memory_used[10m], 600)"
                  "expr"  "predict_linear(jvm_memory_used[10m], 600)"
                  "x" 6  "y" 0)
            (dict "title" "quantile_over_time(0.5, jvm_thread_count[5m]) — median"
                  "expr"  "quantile_over_time(0.5, jvm_thread_count[5m])"
                  "x" 12 "y" 0)
            (dict "title" "quantile_over_time(0.95, jvm_memory_used[10m]) — p95"
                  "expr"  "quantile_over_time(0.95, jvm_memory_used[10m])"
                  "x" 18 "y" 0)
            (dict "title" "double_exponential_smoothing(jvm_thread_count[5m], 0.5, 0.5)"
                  "expr"  "double_exponential_smoothing(jvm_thread_count[5m], 0.5, 0.5)"
                  "x" 0  "y" 8)
            (dict "title" "double_exponential_smoothing(jvm_memory_used[5m], 0.8, 0.3)"
                  "expr"  "double_exponential_smoothing(jvm_memory_used[5m], 0.8, 0.3)"
                  "x" 6  "y" 8)
        }}
        {{- range $i, $p := $panels }}
        {{- if $i }},{{ end }}
        {
          "datasource": {"type": "prometheus", "uid": "snowmelt"},
          "id": {{ add $i 1 }},
          "type": "timeseries",
          "title": {{ $p.title | toJson }},
          "gridPos": {"h": 8, "w": 6, "x": {{ $p.x }}, "y": {{ $p.y }}},
          "options": {"legend": {"displayMode": "list", "placement": "bottom"}, "tooltip": {"mode": "single"}},
          "targets": [{
            "datasource": {"type": "prometheus", "uid": "snowmelt"},
            "expr": {{ $p.expr | toJson }},
            "refId": "A", "legendFormat": ""
          }]
        }
        {{- end }}
      ]
    }
{{- end }}
