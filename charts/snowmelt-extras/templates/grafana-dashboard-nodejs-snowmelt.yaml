{{- if and .Values.observability.grafana.enabled .Values.observability.prometheus.enabled }}
# Grafana dashboard for nodejs, wired against the
# `snowmelt` datasource UID. Companion: `grafana-dashboard-nodejs-prometheus.yaml`.
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "snowmelt.fullname" . }}-grafana-dashboard-nodejs-snowmelt
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "snowmelt.labels" . | nindent 4 }}
    grafana_dashboard: "1"
data:
  nodejs-snowmelt.json: |-
{{ .Files.Get "files/nodejs-snowmelt.json" | indent 4 }}
{{- end }}
